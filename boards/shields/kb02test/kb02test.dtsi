/*
 * kb02test — split 検証用（XIAO nRF52840 Plus ×2、各 2×2 仮マトリクス）
 * 左右共通部：kscan（行ピン）と matrix transform。列ピンは左右の overlay で指定。
 */

#include <dt-bindings/zmk/matrix_transform.h>

/ {
    chosen {
        zmk,kscan = &kscan0;
        zmk,matrix-transform = &default_transform;
    };

    // 左 2 列 ＋ 右 2 列 = 4 列 × 2 行。右 overlay で col-offset = <2> を足す
    default_transform: keymap_transform_0 {
        compatible = "zmk,matrix-transform";
        columns = <4>;
        rows = <2>;
        map = <
            RC(0,0) RC(0,1)   RC(0,2) RC(0,3)
            RC(1,0) RC(1,1)   RC(1,2) RC(1,3)
        >;
    };

    kscan0: kscan0 {
        compatible = "zmk,kscan-gpio-matrix";
        wakeup-source;
        diode-direction = "col2row";

        row-gpios
            = <&gpio0 2 (GPIO_ACTIVE_HIGH | GPIO_PULL_DOWN)>   /* ROW0 = D0 P0.02 */
            , <&gpio0 3 (GPIO_ACTIVE_HIGH | GPIO_PULL_DOWN)>   /* ROW1 = D1 P0.03 */
            ;
    };
};
